[project]
name = "lost-found-bot"
version = "5.0.0"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot==20.7",
    "fastapi==0.109.0",
    "asyncpg==0.29.0",
    "python-dotenv==1.0.0",
    "httpx==0.26.0",
]

[tool.vercel]
entrypoint = "api.webhook:app"
